{
  "name": "backend",
  "version": "1.0.0",
  "packageManager": "bun@1.2.20",
  "main": "dist/index.js",
  "scripts": {
    "dev": "bun run ts-node-dev --respawn src/index.ts",
    "build": "bun run tsc",
    "start": "bun run node dist/index.js",
    "lint": "bun run eslint .",
    "test": "bun run vitest --coverage"
  },
  "dependencies": {
    "express": "^5.1.0",
    "api-football-client": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/node24": "^24.0.1",
    "@types/express": "^5.0.3"
  }
}
